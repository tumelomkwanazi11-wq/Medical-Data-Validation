# Medical-Data-Validation
Medical Records Validator (Python)

This Python project validates structured medical records to ensure they follow expected formats and constraints. The validator:

Checks data types for patient ID, age, gender, diagnosis, medications, and last visit ID

Ensures patient ID and visit ID match the required format

Validates age (≥18), gender (male/female), and medications list

Returns a summary of all invalid records with their indices and problematic fields

Skills Demonstrated

Python functions and parameter unpacking

Conditional logic and data validation

Regex pattern matching

Error reporting and summary generation

Handling lists of dictionaries
